/target
**/*.rs.bk
# Cargo.lock is committed: this is an installable binary, so we want reproducible builds.
*.log
flows.jsonl         # raw mitmdump capture scratch
/scratch
.DS_Store
__pycache__/        # mitmproxy compiles the bundled addon (assets/mitm_flows.py)
*.pyc
